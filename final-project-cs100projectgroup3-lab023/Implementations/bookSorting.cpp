#include <vector>
#include <Library.h>

using namespace std;


void Library::sortByAlphabet(const vector<Book> bookCollection&) {
	sort(bookCollection.begin(), bookCollection.end();

}

void Library::sortByRating(const vector<Book> bookCollection&){
	sort(bookCollection.averageRating, bookCollection->ReviewList->averageRating.end());
}

void Library::sortByPopularity(const vector<Book> bookCollection&) {
	sort(bookCollection.popularity.begin(), bookCollection.popularity.begin());
}

void Library::sortByAuthor(const vector<Book> bookCollection&) {
	sort(bookCollection.author.begin(), bookCollection.author.begin());
}


